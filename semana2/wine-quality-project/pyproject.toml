[project]
name = "wine-quality"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "JAANDR7", email = "jaandr7@mapfre.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.5",
    "scikit-learn>=1.9.0",
]

[project.scripts]
wine-quality = "wine_quality:main"

[build-system]
requires = ["uv_build>=0.10.5,<0.11.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.4",
]
